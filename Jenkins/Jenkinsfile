pipeline {
    agent any

    stages {
        stage('Clonar Código') {
            steps {
                git branch: 'main', credentialsId: 'github-credentials', url: 'https://github.com/JavierRodriguezFontiveros/Pruebas_DataScience.git'
            }
        }

        stage('Instalar Dependencias') {
            steps {
                sh 'pip install -r Jenkins/requirements.txt'
            }
        }

        stage('Entrenar Modelo') {
            steps {
                sh 'python Jenkins/train.py'
            }
        }

        stage('Ejecutar Pruebas') {
            steps {
                sh 'pytest Jenkins/test.py'
            }
        }
    }
}


